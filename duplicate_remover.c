#include<stdio.h>
#include<math.h>
void main()
{
    printf("Enter the length of the array from where you want to remove the duplicate element:");
    int len;
    scanf("%d",&len);
    int j=1;
    while(j>0)
    {
        if(len>0)
            break;
        else{
            printf("Invalid input!, enter the length of the array again:");
            scanf("%d",&len);
            j++;
        }
    }
    int 
    int c=0;
    for(int i=0;i<len;i++)
    {

    }
}